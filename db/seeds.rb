# This file should contain all the record creation needed to seed the
# database with its default values. The data can then be loaded with
# `rake db:seed` (or created alongside the db with `rake db:setup`).
Seeds.load
